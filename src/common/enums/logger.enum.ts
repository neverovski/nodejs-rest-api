export enum LoggerCtx {
  AJV = 'Ajv',
  APPLE = 'Apple',
  CACHE = 'Cache',
  EMAIL = 'Email',
  FACEBOOK = 'Facebook',
  GITHUB = 'GitHub',
  GOOGLE = 'Google',
  HTTP = 'HTTP',
  SERVICE = 'Service',
  SQL = 'SQL',
}

export enum LogLevel {
  DEBUG = 'debug',
  ERROR = 'error',
  INFO = 'info',
  TRACE = 'trace',
  WARN = 'warn',
}

export enum LogClient {
  CONSOLE = 'console',
  ECS = 'esc',
  SEQ = 'seq',
}
